# Deployment Rules — MyPortfolio

No deployment target has been decided yet.

When a target is decided, document here:
- Hosting platform (e.g. Vercel, Netlify, GitHub Pages)
- Build configuration required
- Environment variables needed in production
- Deploy commands or CI/CD setup
