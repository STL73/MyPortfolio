# API Rules — MyPortfolio

No API exists in this project currently.

When an API is added, document here:
- Endpoint pattern (e.g. /api/v1/resource)
- Validation library and approach
- Error response shape
- Auth/token handling
